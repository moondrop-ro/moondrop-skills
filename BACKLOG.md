# Backlog

## Infrastructure
- [ ] Extract version check preamble into a shared include or template system to avoid drift across skills
- [ ] Add Windows path detection to repo discovery in the preamble (`D:/projects/...` vs `$HOME/projects/...`)
